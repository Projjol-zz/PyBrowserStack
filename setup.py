from setuptools import setup

setup(name='browser_wrapper',
      version='0.1',
      description='BrowserStack API wrapper in python',
      author='Projjol Banerji',
      packages=['browser_wrapper'],
      zip_safe=False)
